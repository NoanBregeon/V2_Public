const { SlashCommandBuilder, EmbedBuilder } = require('discord.js'); module.exports={ data:new SlashCommandBuilder().setName('help').setDescription('Afficher l\'aide').setDMPermission(false), async execute(i){ const cmds=i.client.moduleManager.get('commandHandler').commands; const list=[...cmds.keys()].sort().map(n=>`• /${n}`).join('\n'); const embed=new EmbedBuilder().setTitle('📋 Commandes').setDescription(list).setColor(0x5865F2); await i.reply({ embeds:[embed], flags:64 }); } };
