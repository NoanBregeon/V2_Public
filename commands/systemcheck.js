const { SlashCommandBuilder } = require('discord.js'); module.exports={ data:new SlashCommandBuilder().setName('systemcheck').setDescription('Vérifier l\'état des systèmes').setDMPermission(false), async execute(i){ await i.reply({ content:'🩺 Slash OK. Twitch/Helix nécessite TWITCH_CLIENT_ID + TWITCH_USER_TOKEN + STREAMER_USERNAME. Tickets & voix prêts.', flags:64 }); } };
