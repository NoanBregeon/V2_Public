const { SlashCommandBuilder } = require('discord.js'); module.exports={ data:new SlashCommandBuilder().setName('ping').setDescription('Vérifier la latence').setDMPermission(false), async execute(i){ const sent=await i.reply({content:'Pinging...', fetchReply:true}); const latency=sent.createdTimestamp - i.createdTimestamp; const api=Math.round(i.client.ws.ping); await i.editReply(`🏓 Pong !\n📡 Latence: **${latency}ms**\n💻 API: **${api}ms**`); } };
